#include <stdlib.h>
#include <stdio.h>

typedef struct Node
{
    int number;
    struct Node *next;
} Node;

int main(int argc, char *argv[])
{
    Node *list = NULL;
    
    for (int i = 0; i < argc; i++)
    {
        
    }
}